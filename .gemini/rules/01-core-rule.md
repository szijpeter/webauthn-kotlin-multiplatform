# Gemini Core Rule

Primary policy: `docs/ai/STEERING.md`.

Required defaults:

1. Use small, targeted changes.
2. Run fast checks before strict checks.
3. Preserve standards-first and KMP layering constraints.
4. Update spec trace notes when validation semantics change.
