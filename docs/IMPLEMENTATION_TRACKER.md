# Implementation Tracker

This tracker is the canonical machine-readable queue for next-step agent execution.

| ID | Phase | Priority | Status | Modules | Goal | Acceptance | AgentPrompt |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P1-001 | Phase 1 | P1 | DONE | webauthn-server-core-jvm,webauthn-core | Enforce strict Related Origins opt-in in finish flows. | Registration and authentication accept related origins only when extension was explicitly requested. | Implement strict Related Origins opt-in in server finish flows and add negative plus positive tests for both ceremonies. |
| P1-015 | Phase 1 | P1 | DONE | webauthn-client-prf-crypto,webauthn-client-ios,webauthn-network-ktor-client,samples:compose-passkey,docs,tools/agent | Add a published PRF crypto client module and wire PRF extension transport end-to-end. | New `webauthn-client-prf-crypto` API is published and BOM-managed, iOS PRF extension inputs/outputs round-trip through platform bridge, start payload extensions pass through network client, and compose sample demonstrates caller-owned-salt PRF encrypt/decrypt flow with coverage updates. | Implement a two-layer PRF crypto API (low-level + session facade), wire iOS/network extension transport behavior with tests, enrich compose sample with PRF demo actions, and update published-surface docs plus changed-module quality-gate mapping. |
| P1-002 | Phase 1 | P1 | DONE | webauthn-serialization-kotlinx,webauthn-server-ktor | Make extension DTO parsing validation-based and 400-safe in routes. | Invalid extension payloads return ValidationResult.Invalid and route start handlers return HTTP 400. | Add validated extension mapping APIs in WebAuthnDtoMapper, route-level handling, and test invalid payload behavior. |
| P1-003 | Phase 1 | P2 | DONE | webauthn-core | Complete PRF extension missing-output validation logic. | Registration fails when PRF output is missing and authentication fails when PRF results are missing. | Tighten WebAuthnExtensionValidator PRF checks and extend tests for missing output cases. |
| P1-004 | Phase 1 | P2 | DONE | webauthn-server-jvm-crypto | Enable deterministic FIDO U2F verifier coverage with fixtures. | Happy-path and core negative-path tests execute without Ignore annotations. | Replace ignored U2F test with fixture-based cert and key resources and add negative cases. |
| P1-005 | Phase 1 | P2 | DONE | docs,spec-notes,tools/agent | Align steering, roadmap, implementation status, and spec map with implemented semantics and tracker workflow. | Strict changed-scope quality gate passes with trace docs updated and next-step workflow documented. | Update STEERING, WORKFLOWS, IMPLEMENTATION_STATUS, ROADMAP, and spec validation map to reflect implemented extension and attestation behavior, then run strict changed-scope quality gate. |
| P2-001 | Phase 2 | P2 | DONE | webauthn-server-core-jvm,webauthn-server-ktor | Add persistence-backed race and replay integration scenarios beyond in-memory smoke tests. | Deterministic tests cover store-backed replay and concurrent finish behavior. | Implement integration-level persistence race tests for registration and authentication finish flows and map new guarantees into status docs. |
| P3-001 | Phase 3 | P3 | DONE | webauthn-client-android,webauthn-client-ios,webauthn-client-core | Harden platform runtime edge handling for lifecycle and error propagation. | Android and iOS adapters map cancellation/platform failures consistently, reject malformed option inputs deterministically, and preserve coroutine cancellation propagation. | Preserve coroutine cancellation passthrough in shared orchestration while extending Android/iOS lifecycle and error-path tests for deterministic invalid-options vs platform-failure outcomes. |
| P2-002 | Phase 2 | P2 | DONE | webauthn-server-jvm-crypto | Harden Android Key Attestation verification logic. | Verifier strictly checks key purpose, algorithm, size, digests, and origin. | Implement strict checks in AndroidKeyAttestationStatementVerifier and verify with comprehensive tests. |
| P1-006 | Phase 1 | P1 | DONE | webauthn-serialization-kotlinx,webauthn-server-jvm-crypto | Expand CBOR/COSE conformance vectors for malformed and unsupported inputs. | Deterministic vector tests cover malformed CBOR maps, unsupported COSE key shapes, and strict rejection behavior. | Add deterministic CBOR/COSE conformance vectors in serialization and JVM crypto modules, then tighten rejection assertions for malformed inputs. |
| P2-003 | Phase 2 | P2 | DONE | webauthn-server-core-jvm,webauthn-server-ktor | Add external-store contract testing beyond in-memory stores. | At least one non-in-memory store adapter passes replay/race invariants for registration and authentication finish flows. | Build store-contract tests that run the existing race/replay scenarios against a non-in-memory store implementation and document guarantees. |
| P3-002 | Phase 3 | P1 | DONE | webauthn-client-android,webauthn-client-ios | Implement platform response parsing and success-path propagation. | Android and iOS clients return PasskeyResult.Success for valid platform payloads and return deterministic parse errors for malformed payloads. | Implement response payload parsing in Android/iOS adapters, add success-path tests with fixture payloads, and keep cancellation/platform error mapping intact. |
| P3-003 | Phase 3 | P3 | DONE | webauthn-client-android,webauthn-client-ios,docs | Adopt kmmresult for internal error pipelines. | Android and iOS clients use KmmResult internally while strictly keeping PasskeyResult at the boundary. Docs are updated. | Adopt KmmResult internally without leaking it to the public API contract, updating steering and dependency decisions. |
| P1-007 | Phase 1 | P1 | DONE | webauthn-model,webauthn-serialization-kotlinx | Add client-facing protocol fields needed for runtime platform parity. | Creation and response DTO/model mappings support authenticator attachment, attestation conveyance, and authenticator transports with validation coverage. | Extend model/DTO fields and mapper validation for attachment, attestation, and transports, then update tests. |
| P1-008 | Phase 1 | P1 | DONE | webauthn-client-core,webauthn-client-android,webauthn-client-ios | Move ceremony orchestration into shared client core and keep platform adapters thin. | Shared client-core performs option JSON handling, response parsing/validation, and deterministic error mapping while Android/iOS only bridge to platform APIs. | Implement DefaultPasskeyClient and PasskeyPlatformBridge contracts; delegate Android/iOS clients through shared core and add shared tests. |
| P1-009 | Phase 1 | P1 | DONE | webauthn-network-ktor-client | Add backend contract interop to unblock client development against external servers. | Client transport supports default backend contract integration with deterministic mapping and tests. | Add KtorPasskeyServerClient with explicit backend contract injection and test contract-specific behavior. |
| P1-010 | Phase 1 | P1 | DONE | docs,samples:backend-ktor,samples:compose-passkey | Finalize client-first execution path with default backend contract and association guidance. | Team can run client flows without first-party backend release hardening by using `samples/backend-ktor` and default contract sample wiring, with clear setup docs. | Update client-first execution docs, wire samples to default backend contract, and provide association-file guidance and helper automation for `samples/backend-ktor`. |
| P1-011 | Phase 1 | P1 | DONE | webauthn-client-core,webauthn-client-json-core,webauthn-client-compose,webauthn-client-android,webauthn-client-ios,webauthn-network-ktor-client | Split typed core from optional JSON path and add Compose integration while closing interop review regressions. | Typed `PasskeyClient` no longer requires codec wiring; optional JSON APIs remain available via `webauthn-client-json-core`; compose module exposes remember/state helpers; transport contract regressions are covered by tests. | Break `PasskeyClient`/`PasskeyPlatformBridge` into typed flow, add optional JSON module and compose helpers, update Android/iOS adapters, and close PR feedback with transport regression tests. |
| P1-012 | Phase 1 | P2 | DONE | webauthn-client-ios,docs | Implement real iOS security-key capability support for client readiness. | iOS bridge supports external security-key ceremonies via `ASAuthorizationSecurityKeyPublicKeyCredentialProvider` path and flips `supportsSecurityKey` to true only when that path is wired and tested. | Keep `supportsSecurityKey=false` until security-key provider bridging is implemented, then add provider flow wiring, capability gating, and regression coverage before re-enabling the capability flag. |
| P1-013 | Phase 1 | P1 | DONE | webauthn-serialization-kotlinx,samples | Stabilize compose passkey demo auth matching and reduce debug noise. | Registration/Authentication mapper preserves credential-id consistency by preferring `rawId`; sample diagnostics no longer duplicate each log line. | Prefer `rawId` in registration response mapping, emit registration DTO ids from attested credential data, and remove duplicate dual-sink demo logging. |
| P1-014 | Phase 1 | P1 | IN_PROGRESS | webauthn-server-core-jvm,gradle,docs | Remove `kotlinx-serialization` `1.9.x` pin once Signum compatibility with `1.10.x` is verified. | `serialization` stays pinned at `1.9.0`, captured Android assertion-vector regression tests remain green, and upstream blocker [a-sit-plus/signum#415](https://github.com/a-sit-plus/signum/issues/415) is resolved before unpin. | Keep `1.9.0` pinned and rely on captured vector regression tests to detect 1.10 incompatibility; document DER/P1363 normalization rationale where that fallback is applied; only unpin after upstream compatibility is confirmed. |
| P2-004 | Phase 2 | P2 | DONE | webauthn-server-core-jvm,webauthn-server-jvm-crypto,webauthn-server-ktor | Refactor H2 store duplication and cache certificate trust anchors. | H2 store code is shared via testFixtures and TrustChainVerifiers are initialized via lazy singletons to avoid repeated I/O. | Extract H2StoreTestAdapter to test fixtures and update attestation verifiers to cache TrustChainVerifier. |
| P2-005 | Phase 2 | P2 | DONE | webauthn-server-jvm-crypto,webauthn-server-store-exposed | Address PR feedback regarding malformed certificates and DB schema constraints. | Replaced malformed GlobalSign R2 PEM with standard bundle version and widened `origin_value` column to 1024 lengths in Exposed. | Replace `gs_root_r2.pem` and expand `ExposedStores.kt` origin lengths. |
| P2-006 | Phase 2 | P1 | DONE | webauthn-model,webauthn-serialization-kotlinx,webauthn-crypto-api,webauthn-server-core-jvm,webauthn-server-jvm-crypto | Remove public raw-array value semantics from shared protocol and crypto contracts. | Shared protocol/crypto value objects use `Base64UrlBytes` plus fixed-size domain wrappers, serialization preserves the same wire format, and targeted regression tests plus changed-scope quality gates pass. | Replace public `ByteArray`-backed model fields with `Base64UrlBytes`, `RpIdHash`, and `Aaguid`, migrate serializers and crypto/server contracts, and cover equality plus invariant behavior with targeted tests and docs updates. |
| P5-001 | Phase 5 | P1 | DONE | docs,tools/agent,.github | Switch the repo into release mode with PR-centric enforcement and a temporary execution map. | Steering, workflows, hooks, CI, and launch/security docs all reflect PR-first release mode and the temporary first-release plan file existed during release-prep. | Update steering/workflow docs, make local pre-push advisory, keep PR CI blocking, add `docs/ai/FIRST_PUBLIC_RELEASE_PLAN.md` for the active effort, and align public launch/security documentation. |
| P5-002 | Phase 5 | P1 | DONE | gradle,build-logic,platform:bom,webauthn-* | Add compatibility validation and Maven Central publishing infrastructure for the curated public surface. | `GROUP`/`VERSION_NAME` metadata exist, BCV baselines are committed for supported published modules, and `publishToMavenLocal` plus Central Portal tasks resolve for the published surface. | Add BCV, Vanniktech publish conventions, Maven Central workflow wiring, release metadata, and initial API baselines for the publishable modules plus the BOM. |
| P5-003 | Phase 5 | P1 | DONE | README.md,docs,webauthn-*,platform/*,samples/* | Improve OSS-facing documentation and module-level adoption guidance. | Root README explains value and integration paths, each module has a README, and community-health plus maintainer release docs exist. | Rewrite the root README, add module READMEs, add `CHANGELOG.md`/`CONTRIBUTING.md`/PR template/`docs/MAVEN_CENTRAL.md`, and fix public wording mismatches such as Renovate vs Dependabot. |
| P5-004 | Phase 5 | P1 | DONE | gradle,.github,docs | Perform the live first public release cut. | `0.1.0` is published to Maven Central, tag `v0.1.0` exists, post-release verification passes, and the temporary first-release plan file is removed in cleanup. | Completed on 2026-03-12: publish workflow run `23023698117` succeeded, Maven Central coordinates resolve, tag `v0.1.0` and GitHub release were created, and the temporary first-release plan file is removed in this cleanup change. |

## Deferred TODO Backlog (Non-Blocking)

No open deferred TODO items.

Completed on 2026-03-26:

| ID | Phase | Priority | Status | Modules | Goal | Acceptance | AgentPrompt |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P6-001 | Phase 6 | P3 | DONE | samples:backend-ktor,samples:compose-passkey,docs | Add short sample-app recordings (backend + Android + iOS Compose hosts) before broader outreach. | Short recordings are captured and linked from outreach-facing docs in the root README. | Record concise sample-app videos for backend plus Android/iOS Compose hosts that demonstrate registration, sign-in, and PRF flows, then link them from docs used during outreach. |
